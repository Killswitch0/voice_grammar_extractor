# Copy this file to .env and fill in your token:
#   cp .env.example .env
#
# Only needed if your recordings contain other voices besides yours
# (see README, "Quickstart" step 3, for how to get a token).
# Leave it empty/unset if you only ever use --no-diarization.

HF_TOKEN=
